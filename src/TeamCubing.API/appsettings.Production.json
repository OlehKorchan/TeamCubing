{
  "Settings": {
    "JwtSettings": {
      "TokenKey": "{TokenKey}",
      "ExpiresInHours": 168
    },
    "CosmosSettings": {
      "Host": "{Host}",
      "Secret": "{Secret}",
      "Database": "{Database}"
    },
    "SqlSettings": {
      "ConnectionString": "{SqlConnectionString}"
    }
  }
}
